#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter the numbers :\n");
    scanf("%d %d %d",&a,&b,&c);
    int result =a>b?(a>c ? a:c) : (b>c ? b:c);
    printf("%d is the greatest number",result);
    return 0;
}
